/*
 * SPDX-FileCopyrightText: 2015-2026 Espressif Systems (Shanghai) CO LTD
 *
 * SPDX-License-Identifier: Apache-2.0
 */
#ifndef __RPC_SLAVE_IF_H
#define __RPC_SLAVE_IF_H

/* RT-Smart exposes the slave RPC interface as generated protobuf messages.
 * This replaces the ESP-IDF ctrl_cmd_t conversion layer while retaining the
 * upstream core/slaveif/wrap ownership split. */
#include "esp_hosted_rpc_schema.h"

#endif /* __RPC_SLAVE_IF_H */
